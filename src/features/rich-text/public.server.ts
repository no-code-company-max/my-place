import 'server-only'

/**
 * Superficie server-only del slice `rich-text`.
 *
 * Reservado para queries futuras de mention resolvers, búsqueda contextual
 * para autocompletes server-side, etc. Vacío por ahora — F.3+ pueblan.
 */

export {}
