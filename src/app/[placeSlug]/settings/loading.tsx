/**
 * Skeleton de la landing de Settings (`/settings`). Page placeholder
 * hasta Fase 2/4 — un solo bloque de título + descripción. Sin
 * animaciones (CLAUDE.md cozytech).
 */
export default function SettingsLoading() {
  return (
    <div className="p-8" aria-busy="true" aria-live="polite">
      <div className="h-9 w-64 rounded bg-soft" />
      <div className="mt-3 h-4 w-80 rounded bg-soft" />
    </div>
  )
}
